<!-- erb = embedded ruby in html -->
<h1>My Pages</h1>

<% @pages.each do |page| %>
    <%= page.title %>
    <%= link_to 'Show Page', page_path(page) %>
    <% end %>

<!-- non echo
    # <% %>
    #echoing prints on page
  # <%= %>
  -->